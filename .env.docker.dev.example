# ====================================================================
# Arizu Development Docker Environment Configuration
# ====================================================================
#
# This file contains safe development values for running the Docker stack.
# Copy this file to .env.docker.dev and modify as needed.
#
# IMPORTANT: These are development-only values. DO NOT use in production!
# ====================================================================

# PostgreSQL Database Configuration
# ---------------------------------
POSTGRES_USER=arizu_dev
POSTGRES_PASSWORD=dev_password_123
POSTGRES_DB=arizu_dev

# System Configuration
# ---------------------
# Timezone for all containers (use your local timezone for development)
TZ=UTC

# Optional: Uncomment and modify these if you need custom values
# ----------------------------------------------------------------

# PostgreSQL Advanced Settings
# POSTGRES_INITDB_ARGS=--encoding=UTF8 --locale=C

# n8n Advanced Configuration
# N8N_LOG_LEVEL=debug
# N8N_METRICS=true
# N8N_PAYLOAD_SIZE_MAX=16
# EXECUTIONS_TIMEOUT=3600
# EXECUTIONS_TIMEOUT_MAX=7200

# Network Configuration (if you need to change default ports)
# POSTGRES_PORT=5432
# REDIS_PORT=6379
# N8N_PORT=5678

# ====================================================================
# Development Notes:
# ====================================================================
#
# 1. Default database credentials:
#    - Host: localhost:5432
#    - Database: arizu_dev
#    - Username: arizu_dev
#    - Password: dev_password_123
#
# 2. Redis connection:
#    - Host: localhost:6379
#    - No authentication required
#
# 3. n8n web interface:
#    - URL: http://localhost:5678
#    - No authentication required in dev mode
#
# 4. All data is persisted in Docker volumes:
#    - PostgreSQL: arizu_pg_data_dev
#    - Redis: arizu_redis_data_dev
#    - n8n: arizu_n8n_data_dev
#
# ====================================================================